{
  "jupyter.lab.setting-icon": "ui-components:settings",
  "title": "MAAP Jobs Settings",
  "description": "Settings for MAAP Jobs Jupyter extensions.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "maapApiUrl": {
      "type": "string",
      "title": "MAAP API URL",
      "description": "e.g. https://api.maap-project.org/",
      "default": "https://api.maap-project.org/"
    },
    "maapToken": {
      "type": "string",
      "title": "MAAP Token",
      "description": "e.g. jwt:XXXXXXX",
      "default": ""
    }
  }
}
